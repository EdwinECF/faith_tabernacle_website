name: CI

on:
  push:
  pull_request:

jobs:
  validate-html:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v4

      - name: Validate HTML
        run: |
          npm install -g htmlhint
          htmlhint "**/*.html"
